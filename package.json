{
  "name": "chickenhimout",
  "private": true,
  "version": "0.9.0-rc.1",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "tsc --noEmit",
    "qa:first-run": "node scripts/qa.mjs",
    "qa:missions": "node scripts/qa-all-missions.mjs",
    "qa:extras": "node scripts/qa-extras.mjs",
    "qa:offline": "node scripts/qa-offline.mjs",
    "native:sync": "npm run build && cap sync",
    "android": "npm run native:sync && cap open android",
    "ios": "npm run native:sync && cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/core": "^8.4.1",
    "@capacitor/ios": "^8.4.1",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.1",
    "@sparticuz/chromium": "^149.0.0",
    "puppeteer-core": "^25.3.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}
